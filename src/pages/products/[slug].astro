---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import ProductGallery from '../../components/ProductGallery.astro';

export async function getStaticPaths() {
  const products = await getCollection('products');
  return products.map(product => ({
    params: { slug: product.id },
    props: { product },
  }));
}

const { product } = Astro.props;
const lang = 'es';

// Assuming we split the markdown body content by *** to separate ES and EN
const bodyContent = product.body ? product.body.split('***') : [''];
const esContent = bodyContent[0] || '';
---

<BaseLayout title={product.data.title_es} lang={lang}>
  <Header lang={lang} />
  
  <div class="product-hero" style={`background-image: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), url('${product.data.hero_image}')`}>
    <div class="container animate-fade-in">
      <h1>{product.data.title_es}</h1>
      <p class="lead">{product.data.description_es}</p>
    </div>
  </div>
  
  <main class="page-container">
    <div class="container">
      <div class="product-layout">
        <div class="product-visuals">
          {product.data.gallery && product.data.gallery.length > 0 ? (
            <ProductGallery 
              images={product.data.gallery.map(img => ({
                src: img.src,
                alt: img.alt_es
              }))} 
            />
          ) : (
            <img src={product.data.hero_image.replace('-hero', '-detail')} alt={product.data.title_es} class="main-image" />
          )}
        </div>
        
        <div class="product-details">
          <h2>Características Principales</h2>
          {product.data.features_es && product.data.features_es.length > 0 && (
            <ul class="features-list">
              {product.data.features_es.map(feature => (
                <li>{feature}</li>
              ))}
            </ul>
          )}
          
          <div class="product-description" set:html={/* Simple markdown to HTML if needed, but we can just output it as text or use a markdown parser. 
          Actually, let's just render the body content. But Astro handles markdown in content collections. We need to render it. */
          ''} />
          
          <!-- Astro renders body if we use the render() function, but since we concatenated EN and ES, it's easier to just output the raw text for this demo -->
          <div class="description-text">
            {esContent.split('\n\n').map(paragraph => (
              <p>{paragraph}</p>
            ))}
          </div>
          
          <div class="cta-box">
            <h3>¿Interesado en este producto?</h3>
            <p>Contáctanos para una cotización personalizada o asesoría técnica.</p>
            <a href="/contact" class="btn">Solicitar Cotización</a>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <Footer lang={lang} />
</BaseLayout>

<style>
  .product-hero {
    padding: var(--spacing-xl) 0;
    color: var(--color-white);
    background-size: cover;
    background-position: center;
    text-align: center;
  }
  
  .product-hero h1 {
    font-size: 3.5rem;
    margin-bottom: var(--spacing-sm);
  }
  
  .lead {
    font-size: 1.25rem;
    max-width: 800px;
    margin: 0 auto;
    color: var(--color-gray-200);
  }
  
  .page-container {
    padding: var(--spacing-xl) 0;
  }
  
  .product-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-xl);
  }
  
  .main-image {
    width: 100%;
    border-radius: var(--border-radius);
  }
  
  .features-list {
    list-style: none;
    margin-bottom: var(--spacing-md);
  }
  
  .features-list li {
    padding-left: 1.5rem;
    position: relative;
    margin-bottom: 0.5rem;
    font-size: 1.1rem;
  }
  
  .features-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-primary);
    font-weight: bold;
  }
  
  .description-text p {
    margin-bottom: 1rem;
    line-height: 1.7;
    color: var(--color-gray-800);
  }
  
  .cta-box {
    background-color: var(--color-gray-100);
    padding: var(--spacing-md);
    border-radius: var(--border-radius);
    margin-top: var(--spacing-lg);
    border-left: 4px solid var(--color-primary);
  }
  
  .cta-box h3 {
    margin-bottom: 0.5rem;
  }
  
  .cta-box p {
    margin-bottom: 1rem;
  }
  
  @media (max-width: 992px) {
    .product-layout {
      grid-template-columns: 1fr;
    }
    
    .product-hero h1 {
      font-size: 2.5rem;
    }
  }
</style>
